{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Flask website (opens browser)",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/app.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "justMyCode": true
    }
  ]
}
